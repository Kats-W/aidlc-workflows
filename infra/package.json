{
  "name": "au-jibun-bank-infra",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "app": "bin/app.ts"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy:dev": "cdk deploy --context env=dev --require-approval never",
    "diff": "cdk diff"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.150.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "~5.5.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "aws-cdk": "^2.150.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}
